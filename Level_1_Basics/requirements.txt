# Default installation pulls in the lightweight core dependencies.
-r requirements-core.txt

# Optional tools live in requirements-optional.txt and can be installed via:
#   pip install -r requirements-optional.txt
