# Install the curated core toolchain for Level 2.
-r requirements-core.txt

# Optional connectors and orchestrators are listed in requirements-optional.txt
#   pip install -r requirements-optional.txt
